# =============================================================================
# nota-fiscal-app — variáveis de ambiente
# Copie este arquivo para .env.local e preencha os valores reais.
# NUNCA versione o arquivo .env.local com valores preenchidos.
# =============================================================================

# URL base do backend (nota-fiscal-api)
# Padrão local: http://localhost:8000
# Em produção: URL pública ou interna do servidor da API
VITE_API_BASE_URL=http://localhost:8000
